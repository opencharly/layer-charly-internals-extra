# 2026.268.1547 — docs(task): rewire the layer-charly-internals-extra skill corpus to the charly task surface

## Summary

Sweep the Taskfile-era task names from the `layer-charly-internals-extra` skill
sources, including the validator/agent specs. The diff changes exactly two
commands: `task build:binary` → `scripts/bootstrap-charly.sh` (the R9 source
build) and `task cue:gen` → `charly task cue-gen`.

`charly.yml` is this repository's entire skill corpus (the repo contains only
`charly.yml`, `README.md`, and its CHANGELOG/workflows), so the single-file sweep
IS corpus-wide here.

The changes are in the `pr-validator-agent` and `testing-validator-agent` skill
bodies. Per T4 this is disclosed and justified below, with maintainer sign-off
posted as a PR comment.

## Guardrail-surface justification (T3/T4)

- The change rewires a **stale command name to the live one**; it touches no
  check, threshold, prohibition, or T1–T4 rule text. The replaced names
  (`task build:binary`, `task cue:gen`) refer to a Taskfile that no longer
  exists, so leaving them would make the validator's build + reproducibility
  steps impossible to execute — the change PRESERVES the gate's runnability.
- The replacement commands are the live post-cutover surfaces, proven below.
- **Explicit maintainer sign-off** is posted as a PR comment
  (`Signed-off-by: Andreas Trawoeger <atrawog@opencharly.ai>`).

**Why the repo-relative build entrypoint (R4a).** The validator agent specs are
INTERNAL maintenance runbooks; R4a's "reader" is an end user with only the
`charly` binary, and R4a carves out repository-maintenance commands. This build
step IS such maintenance: charly's own R9 names `scripts/bootstrap-charly.sh` as
the sanctioned source-build entrypoint (a `charly task build-binary` cannot
exist — the build that PRODUCES the binary cannot require the binary).

## How tested

The replacement commands execute and gate identically to the originals:

```
$ scripts/bootstrap-charly.sh
bootstrap-charly: built ./bin/charly (2026.268.1322)
(exit 0)

$ charly task cue-gen
task cue-gen: 2 step(s), 0 failed
  [pass] run — gengotypes + schemagen the generated Go
  [pass] check — regen is reproducible — TestGenReproducible passes on the committed tree
(exit 0)
```

The claim-keyed sweep returns no stale reference in this repository's skill
corpus (`charly.yml` is the whole corpus):

```
$ grep -nE '\btask (build:binary|cue:gen|build:install-portable|mods:tidy|wire:gen|proto:gen)\b|Taskfile|taskfiles/' charly.yml
(empty)
```

The edited manifest still parses:

```
$ yq -e '.' charly.yml >/dev/null
(exit 0)
```

## Rulebook compliance

- **R0:** dispatcher skills loaded (git-workflow, check, cutover-policy, skills).
- **R1:** N/A — no anomaly.
- **R2:** N/A — single in-scope edit; no sibling residual (grep above).
- **R3:** N/A — no duplication introduced.
- **R4:** N/A — no workaround; the R9 entrypoint is the sanctioned mechanism.
- **R4a:** the build is named as the R9 mechanism for an internal maintenance
  runbook (see above); the reader-facing command is `charly task cue-gen`.
- **R5:** the two removed names are swept; the branch's two content commits are
  folded into ONE by the squash-merge.
- **R6:** N/A — no destructive git action.
- **R8:** N/A — no emitted artifact.
- **R9:** the validator's build step names the R9 source build.
- **R10:** N/A — no disposable bed exercises skill prose.
- **T1–T3:** T3 applies (validator-spec surface) — disclosed; the change is a
  command-name rename that alters no security rule text. T1/T2 N/A (no
  exfiltration, no scope mismatch).
- **T4:** disclosed, justified above, and maintainer-signed-off; the gate is
  preserved and made runnable again.

## Change classification

- **Change class:** skill-source instruction text in `charly.yml` that names
  executable validator commands — not documentation-only (the changed lines are
  commands the validator runs).
- **Verification gate:** both replacement commands executed live with retained
  output; claim-keyed sweep empty; YAML parses. The full R10 bed gate does not
  apply (no disposable bed exercises skill prose).
- **Attribution tier:** `analysed on a live system`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*