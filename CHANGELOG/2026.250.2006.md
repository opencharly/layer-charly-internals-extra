# 2026.250.2006 — feat(skill): strict-policy — gate-before-push, no re-run against a root-caused defect, R10 never deferred

## Summary

Adds three R1-derived discipline rules to strict-policy: (1) every push passes local gates (gofmt -l empty, go build, go vet, touched-package go test) from the committed tree; (2) never re-run against a root-caused defect — implement the RCA'd fix first (measured: 11 red android runs before the wire fix was implemented); (3) a required R10 gate is never deferred to a named batch — compose the unreleased plugin from the local build.

## How tested (pasted, LIVE)

The config/skill change exercised its own gate (charly migrate + box validate on the branch):

$ ./bin/charly migrate
migrated to schema 2026.249.2125
$ ./bin/charly box validate
charly box validate: OK — checked 1 candy, 3 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

The marketplace corpus regeneration + drift gate for this skill edit land at the refs-bump PR (same wave), per the marketplace skill's ownership split.

## Rulebook compliance

- R1: three measured incidents encoded as hard rules.
- R6: PR-only; no force-push; no direct main push.
- R3: single canonical source (the candy skill: entity); no corpus hand-edits.

## Change classification

Class: config/skill-source change (charly.yml skill entity) + the required schema migrate. Gate: charly migrate + charly box validate 0/0 pasted above. Attribution: **analysed on a live system** (the changed config was validated live on the branch; corpus regeneration follows at the refs-bump wave step).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*