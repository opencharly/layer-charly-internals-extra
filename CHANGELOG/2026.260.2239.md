# 2026.260.2239 — docs(pr-validator): accept the model-free bot-body footer form

## Summary of changes

Mirror of the merged A1 amendment (`opencharly/action-review#6`) into the **pr-validator agent
spec** this repo owns. A1 (the authoritative validator prompt) now defines a MODEL-FREE
`<Harness> <Runtime>` footer form for PR bodies emitted by a fixed CI generator; the agent spec
in `charly.yml` still carried only the AI-authored form and would keep rejecting the sync/refresh
bot bodies the authoritative A1 permits.

The added paragraph states: a body emitted by a fixed, model-free `printf/echo` block MUST END
with `*Assisted-by: <Harness> <Runtime> (<confidence>)*` (e.g.
`*Assisted-by: GitHub Actions ubuntu-latest (fully tested and validated)*`); do NOT accept a
fabricated AI model name in the `<Provider Full Model Name>` slot, and do NOT accept an `N/A`
placeholder — AI-authored (AI form) or model-free (this form), never a hybrid.

**Closes / relates to:** mirrors `opencharly/action-review#6` (merged).

**Breaking change + rollback:** none — one added paragraph; revert restores the prior spec.

**Files in the diff** (`git diff --stat origin/main...HEAD`):
```
 charly.yml | 12 ++++++++++++
 1 file changed, 12 insertions(+)
```

## Change class

**candy/box config** (skill/agent-entity documentation).

## How R10-tested — exact gate, commands, and pasted output

- **The gate:** `charly box validate` (schema) plus the non-runtime standards.
- **Eval beds run:** `N/A — skill/agent-entity documentation`.
- **Fresh rebuild:** `N/A — no binary`.
- **Did the CHANGED code path execute live?** `N/A — no code path`.

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output:* `N/A`.

*2. Each changed code path executing:* `N/A`.

*3. Docs-only evidence:*
```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:skills` (edit the owning repo's entity) and
  `/charly-internals:marketplace` (the projection chain) — this repo owns the `pr-validator`
  agent entity.
- **R1 RCA + zero warnings:** RCA'd to the A1 amendment not being mirrored; `charly box validate`
  reports 0 warnings/errors.
- **R2 no out-of-scope:** the mirror is the same theme; nothing parked.
- **R3 no duplication:** the clause lives once in the authoritative action-review prompt; this is
  the agent-spec mirror required by the projection chain.
- **R4 no workaround:** the spec now matches the rulebook the gate enforces.
- **R5 hard cutover + grep:** the added clause references the authoritative source; no identifier
  removed.
- **R6 git safety:** `git status` checked; no force-push.
- **R7 runtime gate:** `N/A`.
- **R8 artifact invariants:** `N/A`.
- **R9 binary == source:** `N/A`.
- **R10 disposable + coverage:** `N/A — documentation change class`.
- **RDD / ADE / SDD:** RDD — the amendment's premise was verified by the two blocked generator
  PRs. ADE — the candy's `description:`/`plan:` unchanged. SDD — `N/A`.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law:** `N/A`.
- **Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go`.
- **CHANGELOG:** this repo keeps `CHANGELOG/`; tag-on-merge writes the entry from this body.

## Attribution tier

`documentation reviewed` — the whole diff is an agent-spec prose addition; `charly box validate`
passes; no code or config behavior changes.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*