# 2026.246.0516 — docs(skills): vm-spec — document the clone source arm + snapshot block

## Summary

Document the `clone` source arm and the declarative `snapshot:` block in the vm-spec skill's Go type reference: `VmSource` carries `FromVm`/`FromSnapshot`/`CloudInitClean`, `VmSpec` carries `Snapshots`, and the build dispatch now validates + executes them.

## How tested

```
$ cd layer-charly-internals-extra && git show --stat HEAD
commit 11bc699 ...
 1 file changed, 19 insertions(+), 2 deletions(-)
```

YAML validated (gopls/yaml: no diagnostics on every edit).

## Rulebook compliance

- **R1:** N/A.
- **R3:** single source (the owning repo's skill: entity).
- **R4:** N/A.
- **R5:** N/A — additive documentation.
- **R6:** feat/ branch, no force-push.
- **R7/R10:** docs-only change class.

## Change classification

- **Change class:** docs (skill corpus source)
- **Verification gate:** YAML validation + git show --stat
- **Attribution tier:** documentation reviewed

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*