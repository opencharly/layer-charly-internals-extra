# 2026.265.1721 — docs(pr-validator-agent): reference the org required workflow + org-ruleset owner

## Summary

Documentation-only sweep of the `pr-validator` agent spec: it now references the **org required workflow** and the **org-ruleset owner**, replacing the retired per-repo `pr-validator.yml` dispatcher and `branch-protection` script.

Claim-keyed sweep of `pr-validator-agent`:

- **Machine gate:** the org required workflow `opencharly/.github/.github/workflows/org-wide-pr-validator-required.yml` (which calls the reusable `pr-validator.yml@main`) — not the per-repo `.github/workflows/pr-validator.yml` dispatcher.
- **The check's context** is the org ruleset's required `validate / validate` check run — stated explicitly, replacing the unqualified "branch-protection context".
- **Guardrail the validator protects:** the `org-ruleset` owner script / org ruleset config — not the retired `branch-protection` script/config.
- **Auto-merge:** dropped the nonexistent `auto-merge` workflow; the validator's own workflow arms native auto-merge (squash) inline.

The reference to the reusable `pr-validator.yml@main` is intentional and current — it is the reusable the org required workflow calls; it is not the retired per-repo dispatcher.

Generated projection regenerated separately (marketplace) once this source lands and tags.

## How tested

Documentation-only change class; no runtime bed.

```console
$ grep -nE "branch-protection script|auto-merge. workflow squash|is the required check" layer-charly-internals-extra/charly.yml
$ echo $?
1
```

(The sweep prints nothing — no stale claim survives in this entity. The pattern keys on the retired claims; the reusable `pr-validator.yml@main` reference is current, so it is deliberately not in the pattern.)

## Rulebook compliance

- **R0 skills:** `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1 RCA:** doc-vs-reality divergence from the org-ruleset cutover; swept claim-keyed.
- **R2 complete:** every variant fixed here.
- **R3 no duplication:** points at the single owner.
- **R5:** the claim-keyed sweep is executed above.
- **R6 git safety:** branch + PR; no force-push (round-2 fix is an appended commit).
- **R7 gate:** the sweep output pasted above.
- **Change class:** documentation-only → `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*